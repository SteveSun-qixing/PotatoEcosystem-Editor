# Phase1 - 项目初始化与基础架构

**周期**: 1 周  
**优先级**: 🔴 最高  
**依赖**: 无  
**状态**: 待开发

---

## 1. 阶段目标

完成项目的基础搭建，包括：
- 创建子 Git 仓库
- 初始化 Vue 3 + TypeScript 项目
- 配置开发工具（ESLint、Prettier、Vitest）
- 安装核心依赖（SDK、组件库等）
- 建立项目目录结构
- 编写基础配置文件

---

## 2. 任务列表

### 2.1 创建子 Git 仓库

**任务 ID**: P1-T01  
**优先级**: 🔴 最高  
**预计工时**: 0.5h

**详细步骤**:
1. 在 Chips-Editor 目录下初始化 Git 仓库
2. 创建 `.gitignore` 文件，排除 node_modules、dist 等
3. 创建初始提交

**验收标准**:
- [ ] Git 仓库初始化成功
- [ ] `.gitignore` 配置正确
- [ ] 初始提交完成

---

### 2.2 初始化 Vue 3 + TypeScript 项目

**任务 ID**: P1-T02  
**优先级**: 🔴 最高  
**预计工时**: 1h

**详细步骤**:
1. 创建 `package.json`，配置项目信息
   ```json
   {
     "name": "@chips/editor",
     "version": "1.0.0",
     "type": "module",
     "scripts": {
       "dev": "vite",
       "build": "vue-tsc && vite build",
       "preview": "vite preview",
       "test": "vitest",
       "test:coverage": "vitest --coverage",
       "lint": "eslint . --ext .vue,.ts,.tsx",
       "lint:fix": "eslint . --ext .vue,.ts,.tsx --fix",
       "format": "prettier --write ."
     }
   }
   ```

2. 创建 `vite.config.ts`
   ```typescript
   import { defineConfig } from 'vite';
   import vue from '@vitejs/plugin-vue';
   import path from 'path';

   export default defineConfig({
     plugins: [vue()],
     resolve: {
       alias: {
         '@': path.resolve(__dirname, 'src'),
       },
     },
   });
   ```

3. 创建 `tsconfig.json`
   ```json
   {
     "compilerOptions": {
       "target": "ES2020",
       "module": "ESNext",
       "lib": ["ES2020", "DOM", "DOM.Iterable"],
       "strict": true,
       "moduleResolution": "bundler",
       "allowImportingTsExtensions": true,
       "resolveJsonModule": true,
       "isolatedModules": true,
       "noEmit": true,
       "jsx": "preserve",
       "baseUrl": ".",
       "paths": {
         "@/*": ["src/*"]
       }
     },
     "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
     "references": [{ "path": "./tsconfig.node.json" }]
   }
   ```

4. 创建 `tsconfig.node.json`
   ```json
   {
     "compilerOptions": {
       "composite": true,
       "skipLibCheck": true,
       "module": "ESNext",
       "moduleResolution": "bundler",
       "allowSyntheticDefaultImports": true
     },
     "include": ["vite.config.ts"]
   }
   ```

**验收标准**:
- [ ] `package.json` 配置完整
- [ ] TypeScript 配置正确
- [ ] Vite 配置正确

---

### 2.3 配置 ESLint 和 Prettier

**任务 ID**: P1-T03  
**优先级**: 🟡 高  
**预计工时**: 0.5h

**详细步骤**:
1. 创建 `.eslintrc.cjs`
   ```javascript
   module.exports = {
     root: true,
     env: {
       browser: true,
       es2021: true,
       node: true,
     },
     extends: [
       'eslint:recommended',
       '@vue/eslint-config-typescript',
       '@vue/eslint-config-prettier/skip-formatting',
     ],
     parserOptions: {
       ecmaVersion: 'latest',
       sourceType: 'module',
     },
     rules: {
       'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
       'no-debugger': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
       '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
     },
   };
   ```

2. 创建 `.prettierrc`
   ```json
   {
     "semi": true,
     "singleQuote": true,
     "tabWidth": 2,
     "trailingComma": "es5",
     "printWidth": 100,
     "bracketSpacing": true,
     "arrowParens": "avoid"
   }
   ```

3. 创建 `.prettierignore`
   ```
   node_modules
   dist
   *.md
   ```

**验收标准**:
- [ ] ESLint 配置正确，运行无错误
- [ ] Prettier 配置正确
- [ ] 代码格式化命令可用

---

### 2.4 配置测试框架 Vitest

**任务 ID**: P1-T04  
**优先级**: 🟡 高  
**预计工时**: 0.5h

**详细步骤**:
1. 创建 `vitest.config.ts`
   ```typescript
   import { defineConfig } from 'vitest/config';
   import vue from '@vitejs/plugin-vue';
   import path from 'path';

   export default defineConfig({
     plugins: [vue()],
     test: {
       globals: true,
       environment: 'jsdom',
       coverage: {
         provider: 'v8',
         reporter: ['text', 'json', 'html'],
         exclude: [
           'node_modules/',
           'tests/',
           '**/*.d.ts',
         ],
       },
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, 'src'),
       },
     },
   });
   ```

2. 创建 `tests/setup.ts` 测试初始化文件
   ```typescript
   import { vi } from 'vitest';
   
   // 全局 mock 设置
   vi.mock('@chips/sdk', () => ({
     ChipsSDK: vi.fn(),
     createSDK: vi.fn(),
   }));
   ```

**验收标准**:
- [ ] Vitest 配置正确
- [ ] 测试命令可运行
- [ ] 覆盖率报告可生成

---

### 2.5 安装核心依赖

**任务 ID**: P1-T05  
**优先级**: 🔴 最高  
**预计工时**: 0.5h

**详细步骤**:
1. 安装 Vue 3 相关依赖
   ```bash
   npm install vue@latest vue-router@latest pinia@latest
   ```

2. 安装薯片生态依赖
   ```bash
   npm install @chips/sdk @chips/components @chips/foundation
   ```

3. 安装开发依赖
   ```bash
   npm install -D vite @vitejs/plugin-vue typescript vue-tsc
   npm install -D vitest @vue/test-utils jsdom @vitest/coverage-v8
   npm install -D eslint prettier @vue/eslint-config-typescript @vue/eslint-config-prettier
   npm install -D @types/node
   ```

**验收标准**:
- [ ] 所有依赖安装成功
- [ ] 无版本冲突
- [ ] `npm install` 无错误

---

### 2.6 创建项目目录结构

**任务 ID**: P1-T06  
**优先级**: 🔴 最高  
**预计工时**: 1h

**详细步骤**:
1. 创建 `src` 目录结构
   ```
   src/
   ├── core/               # 核心模块
   │   ├── index.ts        # 导出
   │   ├── editor.ts       # 编辑器主类
   │   ├── connector.ts    # SDK 连接器
   │   └── state/          # 状态管理
   │       ├── index.ts
   │       └── stores/
   │           ├── editor.ts
   │           ├── card.ts
   │           └── ui.ts
   │
   ├── layouts/            # 布局模块
   │   ├── index.ts
   │   ├── infinite-canvas/
   │   └── workbench/
   │
   ├── components/         # 功能组件
   │   ├── index.ts
   │   ├── window/
   │   ├── file-manager/
   │   ├── edit-panel/
   │   ├── card-box-library/
   │   └── dock/
   │
   ├── plugins/            # 插件系统
   │   ├── index.ts
   │   └── types.ts
   │
   ├── utils/              # 工具函数
   │   └── index.ts
   │
   ├── types/              # 类型定义
   │   ├── index.ts
   │   ├── editor.ts
   │   └── window.ts
   │
   ├── styles/             # 样式
   │   ├── variables.css
   │   └── global.css
   │
   ├── App.vue             # 根组件
   └── main.ts             # 入口
   ```

2. 创建每个目录的 `index.ts` 导出文件

3. 创建基础的 `main.ts`
   ```typescript
   import { createApp } from 'vue';
   import { createPinia } from 'pinia';
   import App from './App.vue';
   import './styles/global.css';

   const app = createApp(App);
   app.use(createPinia());
   app.mount('#app');
   ```

4. 创建基础的 `App.vue`
   ```vue
   <script setup lang="ts">
   import { ref } from 'vue';

   const isReady = ref(false);
   </script>

   <template>
     <div id="chips-editor">
       <div v-if="!isReady" class="loading">
         Loading...
       </div>
       <div v-else class="editor-container">
         <!-- 编辑器内容 -->
       </div>
     </div>
   </template>

   <style scoped>
   #chips-editor {
     width: 100%;
     height: 100vh;
   }
   </style>
   ```

5. 创建 `index.html`
   ```html
   <!DOCTYPE html>
   <html lang="zh-CN">
     <head>
       <meta charset="UTF-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <title>Chips Editor - 卡片编辑引擎</title>
     </head>
     <body>
       <div id="app"></div>
       <script type="module" src="/src/main.ts"></script>
     </body>
   </html>
   ```

**验收标准**:
- [ ] 目录结构完整
- [ ] 所有 index.ts 文件创建
- [ ] 项目可以启动

---

### 2.7 创建基础类型定义

**任务 ID**: P1-T07  
**优先级**: 🟡 高  
**预计工时**: 1h

**详细步骤**:
1. 创建 `src/types/editor.ts`
   ```typescript
   import type { ChipsSDK } from '@chips/sdk';

   /** 编辑器配置 */
   export interface EditorConfig {
     /** SDK 实例 */
     sdk: ChipsSDK;
     /** 默认布局 */
     layout: 'infinite-canvas' | 'workbench';
     /** 调试模式 */
     debug?: boolean;
     /** 自动保存间隔（毫秒） */
     autoSaveInterval?: number;
   }

   /** 编辑器状态 */
   export type EditorState = 'idle' | 'initializing' | 'ready' | 'error' | 'destroyed';

   /** 编辑器事件 */
   export interface EditorEvents {
     'editor:ready': void;
     'editor:error': { error: Error };
     'editor:destroyed': void;
     'card:opened': { cardId: string };
     'card:closed': { cardId: string };
     'card:saved': { cardId: string };
     'card:created': { cardId: string };
     'layout:changed': { layout: string };
   }
   ```

2. 创建 `src/types/window.ts`
   ```typescript
   /** 窗口类型 */
   export type WindowType = 'card' | 'tool' | 'modal';

   /** 窗口状态 */
   export type WindowState = 'normal' | 'minimized' | 'collapsed' | 'cover';

   /** 窗口位置 */
   export interface WindowPosition {
     x: number;
     y: number;
   }

   /** 窗口大小 */
   export interface WindowSize {
     width: number;
     height: number;
   }

   /** 窗口配置 */
   export interface WindowConfig {
     id: string;
     type: WindowType;
     title: string;
     position: WindowPosition;
     size: WindowSize;
     state: WindowState;
     zIndex: number;
     resizable?: boolean;
     draggable?: boolean;
     closable?: boolean;
     minimizable?: boolean;
   }

   /** 卡片窗口配置 */
   export interface CardWindowConfig extends WindowConfig {
     type: 'card';
     cardId: string;
     isEditing: boolean;
     coverRatio?: string;
   }

   /** 工具窗口配置 */
   export interface ToolWindowConfig extends WindowConfig {
     type: 'tool';
     component: string;
     icon?: string;
   }
   ```

3. 创建 `src/types/layout.ts`
   ```typescript
   /** 布局类型 */
   export type LayoutType = 'infinite-canvas' | 'workbench';

   /** 布局配置 */
   export interface LayoutConfig {
     type: LayoutType;
     options?: Record<string, unknown>;
   }

   /** 无限画布配置 */
   export interface InfiniteCanvasConfig extends LayoutConfig {
     type: 'infinite-canvas';
     options?: {
       gridSize?: number;
       minZoom?: number;
       maxZoom?: number;
       defaultZoom?: number;
     };
   }

   /** 工作台配置 */
   export interface WorkbenchConfig extends LayoutConfig {
     type: 'workbench';
     options?: {
       sidebarWidth?: number;
       showPreview?: boolean;
     };
   }

   /** 画布状态 */
   export interface CanvasState {
     zoom: number;
     panX: number;
     panY: number;
   }
   ```

4. 创建 `src/types/index.ts` 统一导出

**验收标准**:
- [ ] 类型定义完整
- [ ] TypeScript 编译无错误
- [ ] 导出结构正确

---

### 2.8 创建 README.md

**任务 ID**: P1-T08  
**优先级**: 🟢 中  
**预计工时**: 0.5h

**详细步骤**:
创建项目 README.md 文件：
```markdown
# Chips Editor - 卡片编辑引擎

薯片生态的核心应用，用于创建和编辑卡片的软件平台。

## 特性

- 🎨 **无限画布布局** - 自由摆放卡片，无限扩展
- 📐 **工作台布局** - 分区窗口，专注编辑
- 📁 **文件管理器** - 管理卡片和箱子文件
- 🧩 **插件系统** - 可扩展的编辑组件

## 技术栈

- Vue 3 + TypeScript
- Pinia 状态管理
- Vite 构建工具
- Electron 桌面封装

## 开发

\`\`\`bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建
npm run build

# 运行测试
npm run test
\`\`\`

## 文档

- [开发计划](./开发计划/00-开发计划总览.md)
- [技术文档](./docs/technical/)

## 许可证

MIT License
```

**验收标准**:
- [ ] README 内容完整
- [ ] 命令可执行

---

### 2.9 验证项目结构

**任务 ID**: P1-T09  
**优先级**: 🔴 最高  
**预计工时**: 0.5h

**详细步骤**:
1. 运行 `npm install`，确保依赖安装成功
2. 运行 `npm run dev`，确保开发服务器启动
3. 运行 `npm run build`，确保构建成功
4. 运行 `npm run test`，确保测试框架工作
5. 运行 `npm run lint`，确保代码检查工作

**验收标准**:
- [ ] 所有命令执行成功
- [ ] 开发服务器可访问
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 错误

---

### 2.10 提交代码

**任务 ID**: P1-T10  
**优先级**: 🔴 最高  
**预计工时**: 0.5h

**详细步骤**:
1. 运行 `git add .`
2. 运行 `git commit -m "feat(init): 项目初始化与基础架构"`
3. 验证提交成功

**验收标准**:
- [ ] 代码已提交
- [ ] 提交信息规范

---

## 3. 任务依赖关系

```
P1-T01 (创建Git仓库)
    ↓
P1-T02 (初始化项目) ──→ P1-T03 (ESLint/Prettier)
    ↓                        ↓
P1-T05 (安装依赖) ←──────────┘
    ↓
P1-T06 (目录结构) ──→ P1-T07 (类型定义)
    ↓                        ↓
P1-T04 (Vitest) ←───────────┘
    ↓
P1-T08 (README)
    ↓
P1-T09 (验证)
    ↓
P1-T10 (提交)
```

---

## 4. 完成标准

- [ ] Git 仓库初始化完成
- [ ] Vue 3 + TypeScript 项目配置完成
- [ ] 开发工具配置完成（ESLint、Prettier、Vitest）
- [ ] 核心依赖安装完成
- [ ] 目录结构创建完成
- [ ] 基础类型定义完成
- [ ] 项目可以正常启动和构建
- [ ] 代码提交完成

---

## 5. 风险与应对

| 风险 | 应对措施 |
|------|----------|
| 依赖版本冲突 | 使用确定的版本号，参考其他项目配置 |
| TypeScript 配置问题 | 参考 Chips-ComponentLibrary 的配置 |
| SDK 不可用 | 先使用 mock，后续替换 |

---

**任务分配**: 子代理-01  
**开始时间**: 待定  
**完成时间**: 待定
